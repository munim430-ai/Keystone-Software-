# Build Skill

Build the website for production.

## Instructions

Run the following and report the output:

```bash
cd /home/user/Keystone-Software-/docs && npm run build 2>&1
```

If the build succeeds, report the dist folder size.
If it fails, show the error and suggest a fix.
