# Dev Server Skill

Start the local development server for the website.

## Instructions

Run the following command and report the local URL:

```bash
cd /home/user/Keystone-Software-/docs && npm run dev
```

Tell the user the dev server is running at http://localhost:3000
